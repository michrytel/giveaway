const GiveAwayHelper = ({title, text}) => {
    return (
        <div className="give__away-helper">
            <h3>{title}</h3>
            <p>{text}</p>
        </div>
    )
}

export default GiveAwayHelper